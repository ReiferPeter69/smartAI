{
  "name": "@obsidian/backend",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint src",
    "test": "vitest",
    "typecheck": "tsc --build --force --dry",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@obsidian/core": "file:../core",
    "@prisma/client": "^7.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "dotenv": "^16.6.1",
    "prisma": "^7.3.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
